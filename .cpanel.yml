---
# Runs when you click "Deploy HEAD Commit" in cPanel → Git Version Control.
# cPanel checks out HEAD into the repository working tree, then runs these
# tasks from the repository root as your cPanel user.
deployment:
  tasks:
    - /bin/bash deploy/cpanel-deploy.sh
